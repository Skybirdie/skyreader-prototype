"use strict"; const CONTRACT_PREFIX = "sr2."; const KEY_LENGTH = 16; const SKYMEDIA_BASE_URL = "https://skyreader-prototype.sliburd81.workers.dev"; /* ========================================================= Open Graph / Social Preview configuration ========================================================= */ const OG_SITE_NAME = "Meditation Mornings"; const OG_DEFAULT_THUMBNAIL = "https://storage.googleapis.com/glide-prod.appspot.com/uploads-v2/mKxnsa8ky8uPGbBKpTyv/pub/lu8ys9mg3bqa2jgeWIFB.webp"; const OG_LOGO = "https://storage.googleapis.com/glide-prod.appspot.com/uploads-v2/mKxnsa8ky8uPGbBKpTyv/pub/Qysgcds56u8OmAi76mCC.webp"; const OG_IMAGE_PATH = "/__sky_og_image"; /* ========================================================= Deterministic KV key generation Must match the key generator used by ShareManager / Glide. ========================================================= */ function fnv1a32(value, seed) { let hash = (0x811c9dc5 ^ seed) >>> 0; for (let i = 0; i < value.length; i++) { hash ^= value.charCodeAt(i); hash = Math.imul(hash, 0x01000193) >>> 0; } return hash >>> 0; } function hex8(value) { return value .toString(16) .padStart(8, "0") .toUpperCase(); } function makeKey(payload) { const hash1 = fnv1a32(payload, 0); const hash2 = fnv1a32(payload, 0x9E3779B9); return hex8(hash1) + hex8(hash2); } /* ========================================================= Validation ========================================================= */ function isValidKey(key) { return ( typeof key === "string" && key.length === KEY_LENGTH && /^[A-Fa-f0-9]{16}$/.test(key) ); } function isValidPayload(payload) { if (!payload) return false; if (!payload.startsWith(CONTRACT_PREFIX)) { return false; } const encoded = payload.slice(CONTRACT_PREFIX.length); if (!encoded) { return false; } return /^[A-Za-z0-9_-]+$/.test(encoded); } /* ========================================================= Response helpers ========================================================= */ function htmlHeaders() { return { "content-type": "text/html; charset=UTF-8", "cache-control": "no-store, no-cache, must-revalidate", "pragma": "no-cache" }; } function textHeaders() { return { "content-type": "text/plain; charset=UTF-8", "cache-control": "no-store, no-cache, must-revalidate", "pragma": "no-cache" }; } /* ========================================================= Asset request Always serve the application's root index.html. ========================================================= */ function makeCleanAssetRequest(request) {
  const assetUrl =
    new URL(
      "/index.html",
      request.url
    );

  return new Request(
    assetUrl.toString(),
    {
      method: "GET",
      headers: request.headers
    }
  );
}
/* ========================================================= C2.2 Base64URL decoder ========================================================= */ function base64UrlDecode(value) { try { let base64 = value .replace(/-/g, "+") .replace(/_/g, "/"); while (base64.length % 4) { base64 += "="; } const binary = atob(base64); const bytes = new Uint8Array(binary.length); for (let i = 0; i < binary.length; i++) { bytes[i] = binary.charCodeAt(i); } return bytes; } catch (error) { return null; } } /* ========================================================= C2.2 byte decompressor Must remain compatible with the Glide C2.2 compressor. ========================================================= */ function decompressBytes(bytes) { if (!bytes || bytes.length < 2) { throw new Error("C2.2 payload is too short."); } const version = bytes[0]; if (version !== 2) { throw new Error( "Unsupported C2.2 codec version: " + version ); } const windowSize = 4095; const maxLen = 18; const minLen = 3; const output = []; let pos = 1; while (pos < bytes.length) { const flags = bytes[pos++]; for ( let bit = 0; bit < 8 && pos < bytes.length; bit++ ) { const isMatch = (flags & (1 << bit)) !== 0; /* ----------------------------------------------------- Literal byte ----------------------------------------------------- */ if (!isMatch) { output.push(bytes[pos++]); continue; } /* ----------------------------------------------------- Match token requires two bytes. ----------------------------------------------------- */ if (pos + 1 >= bytes.length) { throw new Error( "Incomplete C2.2 match token." ); } const high = bytes[pos++]; const low = bytes[pos++]; const packed = (high << 8) | low; /* ----------------------------------------------------- Reverse the compressor's packing: packed = ((offset - 1) << 4) | (length - maxLen + 15) Since maxLen = 18: low nibble = length - 3 therefore: length = lowNibble + 3 offset = (packed >>> 4) + 1 ----------------------------------------------------- */ const length = (packed & 0x0F) + minLen; const offset = (packed >>> 4) + 1; if ( length < minLen || length > maxLen ) { throw new Error( "Invalid C2.2 match length." ); } if ( offset < 1 || offset > windowSize || offset > output.length ) { throw new Error( "Invalid C2.2 match offset." ); } /* ----------------------------------------------------- Copy from already decoded output. IMPORTANT: This intentionally allows overlapping copies. That is required for LZ-style back-references. ----------------------------------------------------- */ for (let i = 0; i < length; i++) { const sourceIndex = output.length - offset; output.push( output[sourceIndex] ); } } } return new Uint8Array(output); } /* ========================================================= Decode sr2 payload into the original JSON contract. ========================================================= */ function decodeContractPayload(payload) { if (!isValidPayload(payload)) { throw new Error("Invalid sr2 payload."); } const encoded = payload.slice( CONTRACT_PREFIX.length ); const compressed = base64UrlDecode(encoded); if (!compressed) { throw new Error( "Unable to Base64URL-decode sr2 payload." ); } const utf8 = decompressBytes(compressed); const json = new TextDecoder().decode(utf8); return JSON.parse(json); } /* ========================================================= Normalize a contract into an array. ========================================================= */ function normalizeContractArray(contract) { if (Array.isArray(contract)) { return contract; } if ( contract && typeof contract === "object" ) { return [contract]; } return []; } /* ========================================================= Select the item represented by the share URL. ========================================================= */ function getSharedItem(contract, url) { const items = normalizeContractArray(contract); if (!items.length) { return null; } const requestedId = String( url.searchParams.get("id") || "" ).trim(); if (requestedId) { const matchingItem = items.find(item => String( item?.id ?? "" ).trim() === requestedId ); if (matchingItem) { return matchingItem; } } /* * Normal Glide share contracts are expected to contain * the item being shared. If the contract contains multiple * items and no id was supplied, use the first item. */ return items[0]; } /* ========================================================= Clean a possible Glide / Markdown URL wrapper. ========================================================= */ function cleanMediaUrl(value) { if ( value === null || value === undefined ) { return ""; } let text = String(value).trim(); if (!text) { return ""; } /* --------------------------------------------------------- Remove surrounding JSON-style quotes. --------------------------------------------------------- */ if ( text.length >= 2 && text.startsWith('"') && text.endsWith('"') ) { try { const decoded = JSON.parse(text); if (typeof decoded === "string") { text = decoded.trim(); } } catch (_) { text = text.slice(1, -1).trim(); } } /* --------------------------------------------------------- Markdown link: [https://example.com/image.jpg](https://example.com/image.jpg) --------------------------------------------------------- */ const markdownMatch = text.match( /^\s*\[[^\]]+\]\((https?:\/\/[^)]+)\)\s*$/i ); if (markdownMatch) { return markdownMatch[1]; } /* --------------------------------------------------------- If the field contains a URL directly. --------------------------------------------------------- */ const urlMatch = text.match( /https?:\/\/[^\s<>"')]+/i ); if (urlMatch) { return urlMatch[0]; } return ""; } /* ========================================================= HTML escaping ========================================================= */ function escapeHtml(value) { return String(value ?? "") .replace(/&/g, "&amp;") .replace(/</g, "&lt;") .replace(/>/g, "&gt;") .replace(/"/g, "&quot;") .replace(/'/g, "&#39;"); } /* ========================================================= Build Open Graph metadata. ========================================================= */ function buildOgTags(request, key, payload) { let contract = null; try { contract = decodeContractPayload(payload); } catch (error) { console.error( "SkyMedia OG contract decode failed:", error ); return ""; } const url = new URL(request.url); const item = getSharedItem( contract, url ); const title = String( item?.title || OG_SITE_NAME ).trim(); const imageUrl = new URL( OG_IMAGE_PATH, url.origin ); imageUrl.searchParams.set( "k", key ); /* * Preserve id when supplied so that the OG image * endpoint can select the correct item. */ const requestedId = String( url.searchParams.get("id") || "" ).trim(); if (requestedId) { imageUrl.searchParams.set( "id", requestedId ); } const escapedTitle = escapeHtml(title); const escapedSiteName = escapeHtml(OG_SITE_NAME); const escapedImageUrl = escapeHtml( imageUrl.toString() ); const escapedPageUrl = escapeHtml( url.toString() ); let tags = ""; /* * Open Graph title. */ tags += `<meta property="og:title" content="${escapedTitle}">`; /* * Temporary site name. */ tags += `<meta property="og:site_name" content="${escapedSiteName}">`; /* * The actual short share URL. */ tags += `<meta property="og:url" content="${escapedPageUrl}">`; /* * Generated OG image. * * The __sky_og_image endpoint handles the * item's thumbnail and falls back to the * default thumbnail when necessary. */ tags += `<meta property="og:image" content="${escapedImageUrl}">`; /* * Explicit image dimensions for social crawlers. */ tags += `<meta property="og:image:width" content="1200">`; tags += `<meta property="og:image:height" content="630">`; return tags; } /* ========================================================= Inject recovered contract AND OG metadata into index.html. ========================================================= */ function injectContractBootstrap( html, payload, request, key ) { const encodedPayload = JSON.stringify(payload); const ogTags = key ? buildOgTags( request, key, payload ) : ""; const script = ` <script> (function () { "use strict"; var payload = ${encodedPayload}; if (!payload) return; try { var url = new URL(window.location.href); url.searchParams.delete("k"); url.searchParams.set( "contractz", payload ); window.history.replaceState( null, "", url.pathname + "?" + url.searchParams.toString() + url.hash ); } catch (error) { console.error( "SkyMedia KV bootstrap failed:", error ); } })(); </script> `; const marker = "</head>"; const index = html.indexOf(marker); if (index < 0) { return html; } return ( html.slice(0, index) + ogTags + script + html.slice(index) ); } /* ========================================================= Serve index.html with a recovered contract. ========================================================= */ async function serveWithContract( request, env, payload, key = "" ) { const assetResponse = await env.ASSETS.fetch( makeCleanAssetRequest(request) ); if (!assetResponse.ok) { return assetResponse; } const contentType = assetResponse.headers.get( "content-type" ) || ""; if ( !contentType .toLowerCase() .includes("text/html") ) { return assetResponse; } const html =
  await assetResponse.text();

const modified =
  injectContractBootstrap(
    html,
    payload,
    request,
    key
  );

const diagnostic =
  `<!-- SKYMEDIA-WORKER-TEST key=${key || "none"} -->`;

return new Response(
  diagnostic + modified,
  {
    status: 200,
    headers: htmlHeaders()
  }
); return new Response( modified, { status: 200, headers: htmlHeaders() } ); } /* ========================================================= Generate the social-preview image. ========================================================= */ async function serveOgImage( request, env ) { if (!env.IMAGES) { return new Response( "SkyMedia Images binding is not configured.", { status: 500, headers: textHeaders() } ); } const url = new URL(request.url); const key = String( url.searchParams.get("k") || "" ) .trim() .toUpperCase(); if (!isValidKey(key)) { return new Response( "Invalid SkyMedia publication key.", { status: 400, headers: textHeaders() } ); } let payload = null; try { payload = await env.MEDIA_KV.get(key); } catch (error) { console.error( "SkyMedia OG KV read failed:", error ); return new Response( "SkyMedia KV read failed.", { status: 500, headers: textHeaders() } ); } if (!payload) { return new Response( "SkyMedia publication not found.", { status: 404, headers: textHeaders() } ); } if (!isValidPayload(payload)) { return new Response( "SkyMedia publication data is invalid.", { status: 500, headers: textHeaders() } ); } let contract; try { contract = decodeContractPayload(payload); } catch (error) { console.error( "SkyMedia OG C2.2 decode failed:", error ); return new Response( "SkyMedia publication data could not be decoded.", { status: 500, headers: textHeaders() } ); } const item = getSharedItem( contract, url ); let thumbnailUrl = cleanMediaUrl( item?.thumbnail ); /* * If there is no thumbnail, use the temporary * default thumbnail. */ if (!thumbnailUrl) { thumbnailUrl = OG_DEFAULT_THUMBNAIL; } let baseResponse = await fetch(thumbnailUrl); /* * If the supplied thumbnail is unavailable, * fall back to the default thumbnail. */ if ( !baseResponse.ok || !baseResponse.body ) { if ( thumbnailUrl !== OG_DEFAULT_THUMBNAIL ) { baseResponse = await fetch( OG_DEFAULT_THUMBNAIL ); } } if ( !baseResponse.ok || !baseResponse.body ) { return new Response( "SkyMedia OG thumbnail could not be loaded.", { status: 502, headers: textHeaders() } ); } /* * Fetch the logo separately. */ const logoResponse = await fetch(OG_LOGO); if ( !logoResponse.ok || !logoResponse.body ) { return new Response( "SkyMedia OG logo could not be loaded.", { status: 502, headers: textHeaders() } ); } /* * Determine the base image dimensions so that * the logo can be sized relative to the thumbnail. * * tee() allows us to inspect the image and still * pass another stream into the transformation. */ const baseStreams = baseResponse.body.tee(); let imageInfo = null; try { imageInfo = await env.IMAGES.info( baseStreams[0] ); } catch (error) { console.error( "SkyMedia OG image info failed:", error ); } /* * Default logo width is 16% of the base image width. * * Keep reasonable bounds so very small or very * large source images don't produce an absurd logo. */ let logoWidth = 160; if ( imageInfo && Number.isFinite( Number(imageInfo.width) ) ) { logoWidth = Math.round( Number(imageInfo.width) * 0.16 ); logoWidth = Math.max( 80, Math.min( 240, logoWidth ) ); } /* * Use the second tee'd stream for the actual * transformation. */ let imagePipeline = env.IMAGES.input( baseStreams[1] ); /* * Overlay the temporary Meditation Mornings logo * in the bottom-right corner. */ imagePipeline = imagePipeline.draw( env.IMAGES .input( logoResponse.body ) .transform({ width: logoWidth, fit: "contain" }), { bottom: 18, right: 18, opacity: 0.88 } ); const result = await imagePipeline .output({ format: "image/webp", quality: 85 }); return result.response({ headers: { "Cache-Control": "public, max-age=86400, stale-while-revalidate=604800" } }); } /* ========================================================= Worker ========================================================= */ export default { async fetch( request, env, ctx ) { const url = new URL(request.url); const keyParam = url.searchParams.get("k"); const contractz = url.searchParams.get("contractz"); /* ===================================================== SOCIAL PREVIEW IMAGE /__sky_og_image?k=<key> This is intentionally handled before the normal application routes. ===================================================== */ if ( url.pathname === OG_IMAGE_PATH ) { return serveOgImage( request, env ); } /* ===================================================== SHARE PRIMING ENDPOINT This MUST be handled before the normal ?k / ?contractz routes. ===================================================== */ if ( url.pathname === "/__sky_share_prime" ) { const corsHeaders = { "access-control-allow-origin": "*", "access-control-allow-methods": "POST, OPTIONS", "access-control-allow-headers": "Content-Type", "content-type": "application/json; charset=UTF-8", "cache-control": "no-store, no-cache, must-revalidate" }; if ( request.method === "OPTIONS" ) { return new Response( null, { status: 204, headers: corsHeaders } ); } if ( request.method !== "POST" ) { return new Response( JSON.stringify({ error: "Share-prime requires POST." }), { status: 405, headers: corsHeaders } ); } let body; try { body = await request.json(); } catch (_) { return new Response( JSON.stringify({ error: "Invalid share-prime JSON." }), { status: 400, headers: corsHeaders } ); } const primeKey = String( body?.k || "" ) .trim() .toUpperCase(); const primeContract = String( body?.contractz || "" ); const primeSection = String( body?.section || "" ).trim(); const primeId = String( body?.id || "" ).trim(); if ( !isValidKey(primeKey) || !isValidPayload(primeContract) ) { return new Response( JSON.stringify({ error: "Invalid share-prime data." }), { status: 400, headers: corsHeaders } ); } if ( makeKey(primeContract) !== primeKey ) { return new Response( JSON.stringify({ error: "Share-prime key mismatch." }), { status: 400, headers: corsHeaders } ); } try { await env.MEDIA_KV.put( primeKey, primeContract ); const storedPrimeContract = await env.MEDIA_KV.get( primeKey ); if ( storedPrimeContract !== primeContract ) { throw new Error( "KV verification failed." ); } } catch (error) { console.error( "SkyMedia share-prime KV failure:", error ); return new Response( JSON.stringify({ error: "SkyMedia KV write failed." }), { status: 500, headers: corsHeaders } ); } const cleanUrl = new URL( SKYMEDIA_BASE_URL ); cleanUrl.searchParams.set( "k", primeKey ); if (primeSection) { cleanUrl.searchParams.set( "section", primeSection ); } if (primeId) { cleanUrl.searchParams.set( "id", primeId ); } return new Response( JSON.stringify({ url: cleanUrl.toString() }), { status: 200, headers: corsHeaders } ); } /* ===================================================== FIRST USE ?k=<key>&contractz=<payload>&section=...&id=... Store the full contract in KV, then redirect to the clean URL without the contract. ===================================================== */ if ( keyParam && contractz ) { const normalizedKey = keyParam .trim() .toUpperCase(); if ( !isValidKey(normalizedKey) || !isValidPayload(contractz) ) { return new Response( "SkyMedia publication link is invalid.", { status: 400, headers: textHeaders() } ); } const calculatedKey = makeKey(contractz); if ( calculatedKey !== normalizedKey ) { return new Response( "SkyMedia publication link is invalid.", { status: 400, headers: textHeaders() } ); } try { await env.MEDIA_KV.put( normalizedKey, contractz ); } catch (error) { return new Response( "SkyMedia KV write failed.", { status: 500, headers: textHeaders() } ); } let storedPayload = null; try { storedPayload = await env.MEDIA_KV.get( normalizedKey ); } catch (error) { return new Response( "SkyMedia KV verification read failed.", { status: 500, headers: textHeaders() } ); } if ( storedPayload !== contractz ) { return new Response( "SkyMedia KV verification failed.", { status: 500, headers: textHeaders() } ); } /* * Remove only the long contract from the URL. */ const cleanUrl = new URL(request.url); cleanUrl.searchParams.delete( "contractz" ); return Response.redirect( cleanUrl.toString(), 302 ); } /* ===================================================== CLEAN SHORT LINK ?k=<key>&section=...&id=... Retrieve the contract from KV and inject it into index.html, including Open Graph metadata. ===================================================== */ if (keyParam) { const normalizedKey = keyParam .trim() .toUpperCase(); if ( !isValidKey(normalizedKey) ) { return new Response( "SkyMedia publication key is invalid.", { status: 400, headers: textHeaders() } ); } let payload = null; try { payload = await env.MEDIA_KV.get( normalizedKey ); } catch (error) { return new Response( "SkyMedia KV read failed.", { status: 500, headers: textHeaders() } ); } if (!payload) { return new Response( "SkyMedia publication not found.", { status: 404, headers: textHeaders() } ); } if ( !isValidPayload(payload) ) { return new Response( "SkyMedia publication data is invalid.", { status: 500, headers: textHeaders() } ); } /* * IMPORTANT: * Pass the recovered payload and normalized key. * * The key is required here so that * injectContractBootstrap() generates the * Open Graph metadata. */ return serveWithContract( request, env, payload, normalizedKey ); } /* ===================================================== LEGACY DIRECT CONTRACT Existing URLs such as: ?contractz=sr2.... continue to work. ===================================================== */ if (contractz) { if ( !isValidPayload(contractz) ) { return new Response( "SkyMedia contract is invalid.", { status: 400, headers: textHeaders() } ); } /* * Legacy direct-contract URLs do not have * a KV key, so no OG metadata is generated * from this path. */ return serveWithContract( request, env, contractz ); } /* ===================================================== NORMAL REQUEST No KV key and no direct contract. Let Static Assets handle the normal application. ===================================================== */ return env.ASSETS.fetch( request ); } };
